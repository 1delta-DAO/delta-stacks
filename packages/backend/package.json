{
  "name": "@delta-stacks/backend",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:wrangler": "wrangler deploy",
    "log": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@delta-stacks/calldata-sdk-stacks": "workspace:*",
    "@delta-stacks/data-provision": "workspace:*",
    "@stacks/transactions": "^7.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.99.0"
  }
}
