{
  "name": "delta-stacks",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@stacks/clarinet-sdk": "^3.0.0",
    "@stacks/clarinet-sdk-wasm": "^3.0.0",
    "@stacks/transactions": "^7.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0",
    "vitest-environment-clarinet": "^3.0.0"
  }
}
